[project]
name = "mini-agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["mini_agents*"]
exclude = ["examples*"]
